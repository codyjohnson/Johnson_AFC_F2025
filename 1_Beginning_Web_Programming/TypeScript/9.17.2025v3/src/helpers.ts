export const area = (width: number, height: number):number => {
    return height * width;
}

export const perimeter = (width: number, height: number):number => {
    return (width * 2) + (height * 2);
}
